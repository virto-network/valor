[package]
name = "hello-plugin"
version = "0.1.0"
authors = ["Daniel Olano <daniel@olanod.com>"]
edition = "2018"

[dependencies]
valor = { path = "../..", package = "valor-rs" }

[lib]
crate-type = ["dylib"]
